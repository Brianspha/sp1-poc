//! This is the core of the bridge co-processor

#![no_main]
sp1_zkvm::entrypoint!(main);

pub fn main() {
    //let input = sp1_zkvm::io::read();

    //sp1_zkvm::io::commit_slice(Vec::default().as_slice());
}
